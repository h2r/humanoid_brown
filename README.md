# humanoid_brown
code for operating our humanoid platform w/ two kuka arms

# Dependencies
```
sudo apt-get install ros-melodic-libntcan ros-melodic-libpcan
```

# Installation
git clone this repo.    
checkout namespaces branch.    
git submodule init && git submodule update 

